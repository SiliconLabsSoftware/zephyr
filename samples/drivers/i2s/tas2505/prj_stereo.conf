# Overlay applied automatically when CMake is invoked with -DSTEREO=1.
# Switches the EFR32 USART-I2S driver from MONO HW to STEREO HW so
# i2s_configure() accepts channels=2.
CONFIG_I2S_SILABS_EFR32_USART_STEREO=y
